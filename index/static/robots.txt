# robots.txt — gup (Global Updater CLI)
# Site is served from GitHub Pages on the LINDECKER-Charles/gup repo, under the
# /gup/ path. If you change the deploy URL, update the Sitemap directive below,
# the canonical block in index.html, sitemap.xml and llms.txt together.
#
# Default: fully open. For a public OSS landing page, maximum discoverability
# (Google, Bing, DuckDuckGo, plus AI answer engines like ChatGPT, Claude,
# Perplexity and Kagi) is the goal.
#
# If at some point you want to opt out of AI training crawlers, uncomment the
# block at the bottom of this file.

User-agent: *
Allow: /
Disallow:

# Hint that the LLM-oriented manifests exist. Not a standard directive — search
# engines ignore these lines, but a few AI crawlers (You.com, Perplexity, Kagi)
# read robots.txt comments looking for llms.txt references. The paths are
# /gup/-prefixed: publicDir copies these files under the Pages sub-path, not to
# the domain root. The previous "# llms: /llms.txt" pointed at a 404.
# llms: /gup/llms.txt
# llms-full: /gup/llms-full.txt

Sitemap: https://lindecker-charles.github.io/gup/sitemap.xml

# ---------------------------------------------------------------
# Optional: block AI training/scraping crawlers. Uncomment to apply.
# Keeping them ALLOWED (default) maximises discoverability via
# ChatGPT, Claude, Perplexity, Gemini and Kagi answer surfaces —
# which is the whole premise of shipping llms.txt at all.
# ---------------------------------------------------------------
# User-agent: GPTBot
# Disallow: /
# User-agent: ChatGPT-User
# Disallow: /
# User-agent: OAI-SearchBot
# Disallow: /
# User-agent: ClaudeBot
# Disallow: /
# User-agent: Claude-Web
# Disallow: /
# User-agent: anthropic-ai
# Disallow: /
# User-agent: CCBot
# Disallow: /
# User-agent: PerplexityBot
# Disallow: /
# User-agent: Google-Extended
# Disallow: /
# User-agent: Applebot-Extended
# Disallow: /
# User-agent: Bytespider
# Disallow: /
