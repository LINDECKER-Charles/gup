# gup `0.2.1`

> **TL;DR** — Release de maintenance. **Aucun changement fonctionnel** : uniquement des montées de dépendances livrées par Dependabot depuis `0.2.0`, validées par la suite complète (1982 tests, security, coverage). Mêmes flags, même format JSON, même comportement.

`npm i -g @charles_lindecker/gup@0.2.1`

---

## 📦 Dépendances runtime

| Paquet | Avant | Après | Bump |
|---|---|---|---|
| `commander` | `12.1.0` | `15.0.0` | **major ×3** |
| `@inquirer/prompts` | `8.5.0` | `8.5.2` | patch |
| `semver` | `7.8.1` | `7.8.4` | patch |

> ⚠️ **`commander` 12 → 15** est le seul saut majeur côté runtime. L'API utilisée par `gup` (déclaration des commandes/options, sous-commande masquée `__admin-batch`, parsing `provider:packageId`) reste compatible — couverte par les tests CLI et les tests sécu de parsing. Aucun ajustement code nécessaire.

---

## 🛠️ Dépendances dev

| Paquet | Avant | Après | Bump |
|---|---|---|---|
| `@types/node` | `22.10.5` | `25.9.3` | **major ×3** |
| `vitest` | `4.1.6` | `4.1.9` | patch |
| `@vitest/coverage-v8` | `4.1.7` | `4.1.9` | patch |
| `tsx` | `4.19.2` | `4.22.4` | minor |
| `@typescript-eslint/eslint-plugin` | `8.60.0` | `8.61.1` | patch |
| `eslint` | `10.4.0` | `10.4.1` | patch |
| `eslint-plugin-security` | `4.0.0` | `4.0.1` | patch |

> `@types/node` 22 → 25 aligne les types sur des runtimes Node récents. `engines` reste `>=20`, le typecheck (`tsc --noEmit`) passe sans erreur.

---

## 🌐 Site / docs (`index/`)

| Paquet | Avant | Après | Bump |
|---|---|---|---|
| `vite` | `6.4.2` | `8.0.16` | **major ×2** |

Concerne uniquement le build de la landing/doc (`index/`), pas le package npm publié. Sans impact sur le binaire `gup`.

---

## ✅ Vérification

Suite complète relancée sur `node v22.23.0` après `npm ci` :

| Check | Résultat |
|---|---|
| `typecheck` (`tsc --noEmit`) | ✅ |
| `build` (`tsup`) | ✅ `dist/cli.js` 274 KB |
| `lint` (`eslint src`) | ✅ 0 erreur (7 warnings cosmétiques préexistants) |
| `security` (audit-ci + eslint-security + tests/security) | ✅ — 20 tests, 0 vuln ≥ high |
| `test:run` | ✅ **1982 passed** / 53 fichiers |
| `coverage` | stmts **98.58%** · branch **96.54%** · funcs **99.47%** · lines **98.97%** |

`npm audit` remonte 1 vulnérabilité **low** (transitive, sous le seuil `high` d'`audit-ci`) — non bloquante.

---

## ⬆️ Upgrade

```powershell
npm i -g @charles_lindecker/gup@0.2.1
gup --version  # 0.2.1
```

Aucun breaking change utilisateur. Rien à modifier dans tes scripts ou ta CI.

---

## 🔗 Compare

[`0.2.0...0.2.1`](https://github.com/LINDECKER-Charles/gup/compare/0.2.0...0.2.1)

*16 commits — exclusivement `chore(deps)` / `chore(deps-dev)` Dependabot (PR #37, #38, #39, #42, #43, #44, #45, #46).*
